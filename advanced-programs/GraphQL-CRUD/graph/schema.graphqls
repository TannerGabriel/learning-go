# GraphQL schema example
#
# https://gqlgen.com/getting-started/

type Item {
  title: String!
  owner: String!
  rating: Int!
}

type Query {
  items: [Item!]!
}

input NewItem{
  title: String!
  owner: String!
  rating: Int!
}

type Mutation {
  createItems(input: NewItem!): Item!
}